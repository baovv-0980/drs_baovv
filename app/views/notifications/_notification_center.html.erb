<li class="nav-item dropdown">
  <a class="nav-link" data-toggle="dropdown" href="#">
    <i class="far fa-bell"></i>
    <span  id="notification-counter" class="badge badge-warning navbar-badge">
      <%= render "notifications/counter", counter: notifications.count %>
    </span>
  </a>
  <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
    <span class="dropdown-item dropdown-header"><%= render "notifications/counter", counter: notifications.count %> Notifications</span>
    <div id="notification-list">
      <%= render notifications %>
    </div>
    <div class="dropdown-divider"></div>
    <%= link_to "See All Notifications", profiles_path, class: "dropdown-item dropdown-footer" %>
  </div>
</li>
